% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_kanso_map.R
\name{theme_kanso_map}
\alias{theme_kanso_map}
\title{地図（geom_sf）向けの簡素な ggplot2 テーマ}
\usage{
theme_kanso_map(base_size = 11, base_family = "")
}
\arguments{
\item{base_size}{基準フォントサイズ（pt）。}

\item{base_family}{基準フォントファミリ。既定はシステム既定のサンセリフ（\code{""}）。}
}
\value{
ggplot2 のテーマオブジェクト。
}
\description{
\code{\link[=theme_kanso]{theme_kanso()}} を地図描画向けに調整したテーマ。太字タイトル・コンパクトな
凡例・太字の strip といった体裁は引き継ぎつつ、緯度経度の軸テキスト・軸タイトル・
目盛と経緯線（graticule）を取り除き、真っ白な背景にする。
\code{geom_sf()} / \code{coord_sf()} を使った地図に用いる。
}
\details{
スケールバーや北矢印が必要な場合は、別途 \pkg{ggspatial} の
\code{annotation_scale()} / \code{annotation_north_arrow()} を足す。
}
\examples{
\dontrun{
library(ggplot2)
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"))
ggplot(nc) +
  geom_sf(aes(fill = AREA), colour = "white", linewidth = 0.1) +
  scale_fill_viridis_c() +
  labs(title = "North Carolina", fill = "Area") +
  theme_kanso_map()
}
}
